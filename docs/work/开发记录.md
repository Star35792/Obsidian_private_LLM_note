# 开发记录

本文件按时间追加，不替代 Git，也不记录模型隐藏思维链。每条记录只保留能帮助下一次恢复工作的事实。

## 2026-08-18：建立 Agent Harness 基础层

- 变更：新增 Agent Loop、OpenAI-compatible 工具调用解析、Vault 只读工具和 MetadataCache 出入链读取。
- 验证：22 个测试通过，build、lint 和 `git diff --check` 通过。
- 提交：`2f9b2fc`。
- 后续：接入对话 UI、Skill 加载和写工具确认卡片。

## 2026-08-18：迁移文档并建立开发治理

- 变更：将原 `ai/` 平铺文档迁入 `docs/product/` 与 `docs/engineering/`；新增默认入口、流程、权限、工作计划和 memory 引用。
- 验证：迁移后扫描文档内部链接；项目测试、build、lint 和 diff 检查通过。
- 提交：本周期提交后以 Git 历史为准。
- 后续：每个开发周期先更新 [当前计划](当前计划.md)，结束后追加本文件。

## 2026-08-18：验证 AI 流程可执行性

- 变更：补充任务状态机、任务卡最小字段、Start/Plan/Verify/Close Gate、每轮输出契约和验证失败回退规则。
- 判断：原流程适合作为原则，但缺少中间状态和完成证据；本次优化把“知道应该做什么”变成“每一步必须留下什么”。
- 验证：文档相对链接扫描、22 个测试、build、lint 和 `git diff --check` 全部通过。
- 提交：待本周期完成后填写提交哈希。
- 后续：实际开发任务必须从 `TASK-YYYYMMDD-NN` 任务卡开始，连续观察一个周期后再评估是否需要简化。
