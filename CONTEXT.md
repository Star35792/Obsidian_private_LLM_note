# AI 笔记助手

一个内嵌于 Obsidian 的 agent harness：模型在代理循环中自主调用工具操作笔记库，帮助用户把杂乱思维逐步变成可理解、可回看的笔记。

## Language

### 核心循环

**Agent Loop（代理循环）**:
模型的多轮运行方式：每轮可发起工具调用，引擎执行后把结果交还模型，直到模型产出最终结果。本项目的唯一运行时形态。
_Avoid_: 单轮补全、动作流程、固定流水线

**Session（会话）**:
一次持续对话的完整过程，包含用户请求、模型输出、工具调用及其依据、写回结果。会话沉淀为笔记库中的笔记，可搜索可复盘。
_Avoid_: 聊天记录、日志

**Tool（工具）**:
模型在 Agent Loop 中可发起的、由引擎执行的笔记库操作，分为只读工具与写工具两类。
_Avoid_: 函数、API

**只读工具（Read-only Tool）**:
不修改笔记库的工具（搜索、读取、列目录、查链接上下文）。模型可自由调用，无需确认。

**写工具（Mutation Tool）**:
会修改笔记库的工具（创建、更新、移动、移入回收站）。模型可发起调用，但调用必经确认边界拦截，不存在永久删除。
_Avoid_: 变更操作

**确认边界（Confirmation Boundary）**:
本产品的最高不变量：任何写工具的调用都必须经用户确认后才落盘。模型"发起"写工具调用时，引擎将其拦截并转为 ChangePlan 预览，确认后方执行。语义上模型永远不拥有写文件权限。
_Avoid_: 审批弹窗、二次确认

**ChangePlan（变更计划）**:
一次性的、可过期的、可逐项确认的笔记库变更计划，以源笔记内容哈希校验时效性。写工具在确认边界被拦截后的产物。

### 技能

**Skill（技能）**:
一份 Markdown 格式的行为说明（触发条件、工作流、输出格式），模型可在对话中自主加载，用户也可显式触发。原六个固定动作（捕捉/整理/澄清/挑战/行动化/寻找关联）的技能化形态；技能文件存于笔记库中，用户可编辑、可新增。
_Avoid_: 动作（已退役的旧称）、命令、模式

### 笔记与关系

**Proposal（提案）**:
模型产出的结构化结果，永不直接落盘，须经确认边界。语义已迁移：原指单轮 JSON 契约，现指 Agent Loop 末轮的结构化输出。

**整理依据（Rationale）**:
提案中指回原文出处的可审计依据，是链式思考（CoT）的合规替代——不请求、不展示、不伪造模型的隐式推理。

**Vault 地图（Vault Map）**:
注入上下文的轻量笔记库概览（目录树、标题、标签），供模型定位探索起点；不含笔记正文。

**NoteRef（笔记引用）**:
笔记的标识（路径 + 标题 + 别名）。同名笔记必须显式选择路径，绝不静默猜测。

**双链（Bi-link）**:
插件只写前向 `[[wiki 链接]]`；反向链接以 Obsidian MetadataCache 为事实来源。关系优先于相似：关键词重叠只是候选信号，不是建立链接的理由。

### AI 开发

**AI 开发流水线（AI Development Pipeline）**:
本项目中 AI 处理工程任务的完整闭环：阅读上下文、澄清需求、切分方案、实现代码、测试验证、更新仓库内 memory，最后提交 Git。每一步都要留下可复核的产物，不能只依赖对话中的临时上下文。
_Avoid_: 未读先改、未澄清就扩 scope、只跑 happy path、把临时聊天内容当长期记忆。
